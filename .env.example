# Optional. Enables LLM sentiment verification in scripts/scrape-news.mjs.
# Without this, the scraper falls back to keyword-only filtering — still functional.
# Get a free key at https://openrouter.ai/keys
#
# NEVER commit your actual .env file or a real key to the repo.
# In production (GitHub Actions), this is set as a repository secret instead —
# see README.md "Deploy to GitHub Pages" step 3.
OPENROUTER_API_KEY=
